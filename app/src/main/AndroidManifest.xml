<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.halfdotfull.atithi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/KEY" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".login.View.LoginActivity"
            android:theme="@style/NActionBar" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />


        <activity
            android:name=".SplashScreen"
            android:theme="@style/NActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".dashboard.HomeScreenActivity"
            android:label="@string/title_activity_nav"
            android:theme="@style/AppTheme" />
        <activity android:name=".planmytrip.PlanMyTrip" />
        <activity android:name=".WelcomeActivity" />
        <activity android:name=".blog.BLogDetailActivity" />
        <activity android:name=".marketplace.ShopList" />
        <activity android:name=".category.view.Categories" />
        <activity android:name=".AddBlog" />
        <activity android:name=".marketplace.ItemsOfferedList" />
        <activity android:name=".planmytrip.TimeLineActivity"></activity>
    </application>

</manifest>